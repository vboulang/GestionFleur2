<Window x:Class="GestionFleur.Views.InterfaceClient"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestionFleur.Views"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="InterfaceClient" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <TabControl>
            <TabItem Header="Ajouter fleurs individuelles">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="0.5*"/>
                    </Grid.RowDefinitions>
                    <ListView ItemsSource="{Binding Fleurs}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="4" SelectionChanged="ListView_SelectionChanged">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nom" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Prix (CAD)" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding PrixUnitaire, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Couleur" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Couleur, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Description" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="85" >
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListView}}, Path=DataContext.AjouterFleurCommandeCommand }" CommandParameter="{Binding}" Content="Ajouter" Margin="5" FontSize="18" FontFamily="Carlito" Background="LightGreen"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Command="{Binding BoutonRetourCommande}" Margin="5,5,5,5" Grid.Row="4" Grid.Column="3" FontSize="18" FontFamily="Carlito" Background="SeaGreen">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Kind="ArrowReturnLeft" Width="16" Height="16" Margin="0,0,5,0" />
                            <TextBlock Text="Retour" />
                        </StackPanel>
                    </Button>
                </Grid>
            </TabItem>
            <TabItem Header="Ajouter bouquet prédéfinis">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="0.5*"/>
                    </Grid.RowDefinitions>
                    <ListView ItemsSource="{Binding Bouquets}"   Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="4">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nom" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Prix (CAD)" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding PrixUnitaire, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="85" >
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListView}}, Path=DataContext.AjouterBouquetCommandeCommand }" CommandParameter="{Binding}" Content="Ajouter" Margin="5" FontSize="18" FontFamily="Carlito" Background="LightGreen"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Command="{Binding BoutonRetourCommande}" Margin="5,5,5,5" Grid.Row="4" Grid.Column="3" FontSize="18" FontFamily="Carlito" Background="SeaGreen">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Kind="ArrowReturnLeft" Width="16" Height="16" Margin="0,0,5,0" />
                            <TextBlock Text="Retour" />
                        </StackPanel>
                    </Button>
                </Grid>
            </TabItem>
            <TabItem Header="Créer bouquet">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="0.5*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="0.60*"/>
                    </Grid.RowDefinitions>
                    <ListView ItemsSource="{Binding Fleurs}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="5">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nom" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Prix (CAD)" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding PrixUnitaire, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="couleur" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Couleur, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Description" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="70" >
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListView}}, Path=DataContext.AjouterFleurBouquetCommand }" CommandParameter="{Binding}" Content="Ajouter" Margin="5" FontSize="18" FontFamily="Carlito" Background="LightGreen"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Command="{Binding CreerBouquetPersoCommand}" Margin="5,5,5,5" Grid.Row="5" Grid.Column="0" FontSize="18" FontFamily="Carlito" Background="SeaGreen">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Kind="CartCheck" Width="16" Height="16" Margin="0,0,5,0" />
                            <TextBlock Text="Créer Bouquet" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ReinitialiserBouquetCommand}" Margin="5,5,5,5" Grid.Row="5" Grid.Column="1" FontSize="18" FontFamily="Carlito" Background="LightGreen">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Kind="ArrowClockwise" Width="16" Height="16" Margin="0,0,5,0" />
                            <TextBlock Text="Réinitialiser" />
                        </StackPanel>
                    </Button>
                    <Label Content="Message personnalisé:" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Row="0" Grid.Column="3" />
                    <TextBox Text="{Binding BouquetPerso.TempMessageCarte, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="1" Grid.Column="3" Margin="5,5,5,5" Grid.RowSpan="2"/>
                    <Label Content="Contenu du bouquet personnalisé:" HorizontalAlignment="Left" Grid.Row="3" Grid.Column="3" Margin="5,5,5,5" />
                    <ListView ItemsSource="{Binding FleursAAjouterBouquet}" Grid.Row="3" Grid.Column="3" Grid.RowSpan="2" Margin="0,42,0,0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nom" Width="150">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Quantite" Width="80">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding QuantiteEnAttenteBPerso, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Command="{Binding BoutonRetourCommande}" Margin="5,5,5,5" Grid.Row="5" Grid.Column="3" FontSize="18" FontFamily="Carlito" Background="SeaGreen">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Kind="ArrowReturnLeft" Width="16" Height="16" Margin="0,0,5,0" />
                            <TextBlock Text="Retour" />
                        </StackPanel>
                    </Button>
                </Grid>
            </TabItem>
            <TabItem Header="Commande en cours">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="83*"/>
                        <ColumnDefinition Width="83*"/>
                        <ColumnDefinition Width="20*"/>
                        <ColumnDefinition Width="63*"/>
                        <ColumnDefinition Width="83*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="0.5*"/>
                    </Grid.RowDefinitions>
                    <Label Content="Fleurs individuelles:" HorizontalAlignment="Left" Grid.Row="0" Grid.Column="0" Margin="5,5,0,5" />
                    <ListView ItemsSource="{Binding FleursCommandees}" Grid.Column="0" Grid.ColumnSpan="5" Grid.RowSpan="3" Margin="0,42,0,0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nom" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Prix (CAD)" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding PrixUnitaire, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="couleur" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Couleur, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Description" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Quantité Commandée" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding QuantiteEnAttente, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Label Content="Bouquets:" HorizontalAlignment="Left" Grid.Row="3" Grid.Column="0" Margin="5,5,0,5" />
                    <ListView ItemsSource="{Binding BouquetsCommandes}" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="5">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nom" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Prix (CAD)" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding PrixUnitaire, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Message" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding MessageCarte, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Quantité Commandée" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding QuantiteEnAttente, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Command="{Binding AjouterCommandeCommand}" Margin="5,5,4,5" Grid.Row="5" Grid.Column="0" FontSize="18" FontFamily="Carlito" Background="SeaGreen">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Kind="Check" Width="16" Height="16" Margin="0,0,5,0" />
                            <TextBlock Text="Placer commande" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ReinitialiserCommandeCommand}" Margin="6,5,5,5" Grid.Row="5" Grid.Column="1" FontSize="18" FontFamily="Carlito" Background="LightGreen">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Kind="ArrowClockwise" Width="16" Height="16" Margin="0,0,5,0" />
                            <TextBlock Text="Réinitialiser" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding BoutonRetourCommande}" Margin="5,5,5,5" Grid.Row="5" Grid.Column="4" FontSize="18" FontFamily="Carlito" Background="SeaGreen">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Kind="ArrowReturnLeft" Width="16" Height="16" Margin="0,0,5,0" />
                            <TextBlock Text="Retour" />
                        </StackPanel>
                    </Button>
                </Grid>
            </TabItem>
            <TabItem Header="Toutes les commandes">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="0.5*"/>
                    </Grid.RowDefinitions>
                    <Label Content="Commandes:" HorizontalAlignment="Left" Grid.Row="0" Grid.Column="0" Margin="5,5,5,5" />
                    <ListView ItemsSource="{Binding ListeCommandes.Commandes, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="5">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Id" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding CommandeId, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Total (CAD)" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding TotalTransaction, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Paiement effectué?" Width="105">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding PaiementEffectue, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListView}}, Path=DataContext.SupprimerCommandeCommand }" CommandParameter="{Binding}" Content="Annuler" Margin="5" FontSize="18" FontFamily="Carlito" Background="LightGreen"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Command="{Binding BoutonRetourCommande}" Margin="5,5,5,5" Grid.Row="5" Grid.Column="3" FontSize="18" FontFamily="Carlito" Background="SeaGreen">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Kind="ArrowReturnLeft" Width="16" Height="16" Margin="0,0,5,0" />
                            <TextBlock Text="Retour" />
                        </StackPanel>
                    </Button>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>